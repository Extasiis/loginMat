import { makeStyles } from "@material-ui/core/styles"

export default makeStyles({
    transitions: {
        easing: {
            easeInOut: 'linear',
            easeOut: 'linear',
            easeIn: 'linear',
            sharp: 'linear'
        },
    }
})